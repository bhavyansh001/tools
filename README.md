# Flux8Labs Tools

A static directory for Flux8Labs tools: file utilities, web scanners, AI experiments, and internal infrastructure.

## Commands

```bash
npm run start
npm run build
```

Netlify publishes the generated `out/` directory.
